import React, { useState } from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { createPost } from '../../slices/postSlices'

export default function Post() {
   
    return (
        <div>
            <h2>Create a New Post</h2>

        </div>
    )
}
